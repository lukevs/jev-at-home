[project]
name = "jev-at-home"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Luke Van Seters", email = "lukevanseters@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
jev-at-home = "jev_at_home:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
